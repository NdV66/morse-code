class MorseMessage {
    constructor(textMessage = '', morseTextMessage = '') {
        this.textMessage = textMessage;
        this.morseTextMessage = morseTextMessage;
    }
}

export default MorseMessage;
